#include <stdio.h>
int main() {
  int a, b, c;
  int i = 1;
  int count =0;

  while (i <= b) {

    printf("Enter the number  : ");
    scanf("%d", &a);

    if (a == -1) {
      printf("Stopped\n");
      break;
    }
    else if (a>0)
    i++;
  }

 
  printf("%d") return 0;
}