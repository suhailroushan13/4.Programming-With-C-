#include <stdio.h>
int main() {
  int a, b, c;
  int i = 1;
  int count = 0;
  int count1 = 0;
  int count2 = 0;

  while (i <= b) {

    printf("Enter the number  : ");
    scanf("%d", &a);

    if (a == -1) {
      printf("Stopped\n");
      break;
    } else if (a > 0) {
      count += a;


    } 
    sum = a/
    i++;
  }
  printf("Positive Numbers sum is %d\n", count);
  printf("Negative Numbers sum is -%d\n", count1);
  printf("Total 0's are %d\n", count2);

  return 0;
}