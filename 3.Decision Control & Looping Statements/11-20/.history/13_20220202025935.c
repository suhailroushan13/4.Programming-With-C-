#include <stdio.h>
int main()
{
int a,b,c;
printf("Enter the 3 numbers : ");
scanf("%d %d %d",&a,&b,&c);
if(a>b&&a>c)
{
    printf("%d is Greatest Among all",a);
}
else if (b>a&&b>c)
{
  printf("%d is Greatest Among all", b);
}
else if (c>a)
}

return 0;
}