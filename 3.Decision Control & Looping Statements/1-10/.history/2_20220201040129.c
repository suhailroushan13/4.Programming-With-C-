#include <stdio.h>
int main()
{
int num,input;
char ch;
printf("Enter any key :");
scanf("%d", &input);

if (isalpha(ch)>=num )
{
    printf("Number");
    printf("%d",input);
}
else if (input == ch)
{
  printf("Ch");
  printf("%c\n", (char)input);

}

return 0;
}