#include <math.h>
#include <stdio.h>

int main() {
  int x1, x2, y1, y2;
  printf("Enter the valus of x1 and x2 %d %d",x1,x2);
  printf("Enter the valus of y1 and y2 %d %d", y1, x2);

  return 0;
}
