#include <stdio.h>
int main() {
  float d;
  int a, b, c;
  printf("Enter the Int value :");
  scanf("%d", &a);

  a = (float)(d);

  printf("The Float Value now is %f\n", (float)a);
  printf("The Float Value now is %d\n", a);

  return 0;
}