#include <stdio.h>
int main() {
  int a;
  float f;
  printf("Enter int value :");
  scanf("%d", &a);
  a = float(f);

  printf("The Float value is %f",(float)a);
  printf("The Float value is %f", (float)a);

  return 0;
}