#include <stdio.h>
int main()
{
int quantity,price,discount,tax,total_amount;
printf("Enter the price");
scanf("%d",&price);
printf("Enter the quantity");
scanf("%d", &quantity);
p
return 0;
}